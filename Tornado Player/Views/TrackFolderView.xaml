<UserControl x:Class="Tornado.Player.Views.TrackFolderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ViewModels="clr-namespace:Tornado.Player.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance ViewModels:TrackFolderViewModel}"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <Image Height="20"
               Source="{StaticResource Folder}"/>
        <TextBlock Margin="5,0"
                   Text="{Binding TrackFolder.Path, Mode=OneTime}"/>
    </StackPanel>
</UserControl>