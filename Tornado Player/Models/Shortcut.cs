namespace Tornado.Player.Models
{
    public enum Shortcut
    {
        Search,
        TogglePlayback,
        SkipBackward,
        SkipForward,
        VolumeUp,
        VolumeDown,
        ToggleMute
    }
}